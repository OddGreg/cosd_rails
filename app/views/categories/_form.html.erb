<%= simple_form_for(@category) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :book %>
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.input :slug %>
    <%= f.input :image %>
    <%= f.input :visible %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
